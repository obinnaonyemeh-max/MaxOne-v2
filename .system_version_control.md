# Project Version Control & State Document

## Current System State Summary
* **Latest Version:** v1.9.0
* **Last Updated:** 2026-06-09
* **Current Focus/Active Sprint:** Driver Experience App — Welfare Module Refinements & Interactivity

---

## Active Architecture Registry

### Modules Created
* **Driver Experience** — New app module with sidebar navigation, 4 main pages, and 2 sub-pages

### Pages Created
* `Champion360Page` — `/champion-360` — Champion listing with stat cards, DataTable (40 records), filters, search, paginated data slicing
* `ChampionDetailPage` — `/champion-360/:id` — Individual champion detail view
* `TicketManagementPage` — `/ticket-management` — Ticket listing with stat cards, DataTable, filters, TicketDetailSheet
* `CreateTicketPage` — `/ticket-management/create` — Ticket creation form
* `DriverSafetyScorePage` — `/driver-safety-score` — Safety metrics with charts, stat cards, driver risk table
* `WelfarePage` — `/welfare` — Welfare champion management with follow-up queue (Figma-spec cards), champion directory (row-click opens detail sheet), Log Note modal (two-row paired layout, close button), and WelfareDetailSheet integration
* `PerformancePage` — `/performance` — Performance analytics with KPI stat cards, resolver false-resolution breakdown, and category reopen-rate table

### Components Created
* `ChampionInformation` — Champion info card (avatar, name, risk badge, detail rows, contract status badge)
* `ContractInformation` — Contract details card with GSAP count-up and progress bar animation (IntersectionObserver-triggered)
* `WalletInformation` — Wallet/payment info card with GSAP balance count-up animation (IntersectionObserver-triggered)
* `TicketDetailSheet` — Slide-over sheet for ticket detail view
* `DriverDetailSheet` — Slide-over sheet for driver champion detail (safety score, risk level, profile)
* `IncidentChampionsSheet` — Slide-over sheet listing champions matching a selected incident category
* `WelfareDetailSheet` — Slide-over sheet for welfare champion detail (profile, welfare overview with "View All Raised Tickets" CTA, interaction timeline, 3 footer CTAs incl. Log Note wired to modal via `onLogNote` prop)
* `LocationAutocomplete` — Location autocomplete input component
* `MaxIDCard` — MAX ID Card component with Active/Inactive variants, wave pattern header, generation details, action buttons

### Data Files Created
* `mockDriverSafety.ts` — Driver risk records, critical events data, risk level variant maps
* `mockTicketRecords.ts` — Ticket records with status/priority/SLA variant maps
* `mockTicketDetail.ts` — Individual ticket detail data with builder helper
* `mockTicketCategories.ts` — Ticket category definitions

---

## Version History Log

### Version [1.9.0] - 2026-06-09
**Feature: Welfare Module — Follow-up Card Redesign, Sheet & Modal Interactivity Wiring**

**Summary:**
Redesigned the follow-up queue cards to match Figma spec (node 3051-981), wired the Champions Directory table rows to open the WelfareDetailSheet, connected the sheet's "Log Note" footer button to the existing Log Note modal, and refined the modal layout with paired field rows, full-width dropdowns, and a close button.

---

#### 1. Follow-up Queue Card — Figma Redesign

**Files Modified:**

1. `src/pages/WelfarePage.tsx`
   * Replaced vertical two-row card layout with single horizontal row (`flex items-center`)
   * Avatar enlarged from 28px to 40px (initially 54px from Figma, reduced per feedback)
   * Name: 14px, `#121314` color, `-0.14px` letter-spacing
   * Due date: 12px, `#AAAAAA` color, `-0.12px` letter-spacing, 1px margin-top
   * Card: 12px border-radius, `#EAEAEA` border, 16px horizontal / 15px vertical padding
   * "Log Note" button: native `<button>` with `#F3F3F3` background, no border, 8px radius, 38px height, 12.5px text
   * Hover effect: border transitions from `#EAEAEA` to `#c4c4c4` using arbitrary Tailwind property syntax

---

#### 2. Champions Directory Table — Row Click Wiring

**Files Modified:**

1. `src/pages/WelfarePage.tsx`
   * Added `onRowClick={setSelectedChampion}` to `DataTable` in the Champions Directory section
   * Clicking any table row now opens the `WelfareDetailSheet` for that champion

---

#### 3. WelfareDetailSheet — Log Note Button Wiring

**Files Modified:**

1. `src/components/max/WelfareDetailSheet.tsx`
   * Added optional `onLogNote?: (champion: WelfareChampion) => void` prop to `WelfareDetailSheetProps`
   * Wired the footer "Log Note" `Button` to call `onLogNote?.(champion)` on click

2. `src/pages/WelfarePage.tsx`
   * Passed `onLogNote={openLogNote}` prop to `<WelfareDetailSheet>` — clicking "Log Note" in the sheet footer opens the Log Note modal

---

#### 4. Log Note Modal — Layout Refinements

**Files Modified:**

1. `src/pages/WelfarePage.tsx`
   * Reduced modal width from `max-w-xl` to `max-w-lg`
   * Added `w-full` to both Channel and Interaction Type `SelectTrigger` components so dropdowns fill their grid cells (component defaults to `w-fit`)
   * Swapped Follow-up Required and Incident Status positions — Incident Status now on left, Follow-up Required on right
   * Placed Incident Status + Follow-up Required side by side in `grid grid-cols-2 gap-3` (mirroring Channel/Interaction Type row)
   * Follow-up Required toggle: removed `bg-gray-100` background, added `border border-gray-200`, shows "Yes"/"No" label
   * Incident Status `SelectTrigger` given `w-full` to fill its grid cell
   * Added `DialogClose` X button to top-right of modal header (`absolute right-4 top-4`)
   * Added `X` icon import from lucide-react and `DialogClose` import from dialog UI component

---

### Version [1.8.0] - 2026-06-09
**Feature: Performance Analytics Page — Full Build-out**

**Summary:**
Populated the previously blank Performance page (`/performance`) with a complete performance analytics layout. The page displays high-level KPI stat cards, a resolver-level false resolution rate breakdown with ranked horizontal bars, and a category-level reopen rate table with distribution indicators. All metrics are computed dynamically from a mock dataset that references the 12 ticket categories defined in `mockTicketCategories.ts` and the 4 agents from `mockTicketRecords.ts`, ensuring cross-module consistency.

---

#### 1. Performance Page — High-Level Metrics (Top Grid)

**Files Modified:**

1. `src/pages/PerformancePage.tsx`
   * Added imports: `useMemo`, `StatCard`, `Table`/`TableHeader`/`TableBody`/`TableRow`/`TableHead`/`TableCell`, `Badge`, `ticketCategories`
   * 4 `StatCard` components in `grid grid-cols-4 gap-2`:
     - **Total Tickets** (brand-primary indicator) — total count from dataset
     - **Resolved** (success indicator) — resolved count with percentage subtitle
     - **Reopened** (warning indicator) — reopened count with percentage subtitle
     - **False Resolution Rate** (danger indicator) — `reopened / resolved` as percentage, subtitle "Reopened / Resolved"
   * All values derived via `useMemo` from `MOCK_TICKETS` array

---

#### 2. Performance Page — False Resolution Rate by Resolver (Middle Widget)

**Files Modified:**

1. `src/pages/PerformancePage.tsx`
   * Bordered card with section header "False Resolution Rate by Resolver"
   * Ranked list of 4 resolvers (Fatima Bello, Chidi Okafor, Ngozi Eze, Tunde Bakare) sorted by worst false resolution rate
   * Each row: rank number, resolver name (truncated at `w-36`), proportional horizontal bar, percentage value, `Badge variant="outline"` showing `reopened/resolved` counts
   * Bar color thresholds: danger (≥50%), warning (≥30%), success (<30%)
   * Bar widths normalized against the highest resolver rate via `maxResolverRate`
   * Computed via `useMemo` aggregating resolved tickets per resolver

---

#### 3. Performance Page — Reopen Rate by Ticket Category (Bottom Table)

**Files Modified:**

1. `src/pages/PerformancePage.tsx`
   * Bordered card with section header "Reopen Rate by Ticket Category"
   * `Table` component with 5 columns:
     - **Category** — ticket category name
     - **Total** — ticket volume per category
     - **Reopened** — reopened count per category
     - **Reopen Rate** — percentage in color-coded `Badge` (danger ≥40%, warning ≥25%, success <25%)
     - **Distribution** — inline progress bar showing category volume relative to total workload, with percentage label
   * All 12 ticket categories represented, sorted by volume descending
   * Computed via `useMemo` aggregating all tickets per category

---

#### 4. Performance Page — Mock Dataset & Cross-Module Consistency

**Files Modified:**

1. `src/pages/PerformancePage.tsx`
   * `Ticket` interface: `id`, `category`, `resolver`, `resolved` (boolean), `reopened` (boolean)
   * `MOCK_TICKETS` array generated programmatically from `ticketCategories` import (all 12 categories: Incident Safety, Phone Number Update, Documentation, Portfolio, Telematics, Prospect, Fleet, Reverification, Leave Request, Others, Reactivation, HMO/Insurance)
   * Resolvers sourced from `mockTicketRecords` agents: Fatima Bello, Chidi Okafor, Ngozi Eze, Tunde Bakare
   * Deterministic `seededRandom()` function generates stable mock data (5–8 tickets per category, ~12% unresolved rate, ~40% reopen rate among resolved)
   * Categories auto-update if `mockTicketCategories.ts` is modified — no hardcoded category strings

---

### Version [1.7.0] - 2026-06-09
**Feature: Champion Detail Tabs Build-out — Welfare Notes, Time-Off, Summary Widgets & Tab Cleanup**

**Summary:**
Populated two previously placeholder tabs on the Champion Detail page (Welfare Notes and Time-Off) with full data, UI, and interactivity. Added summary widgets to four tabs (Welfare Notes, Tickets, FieldOps, Time-Off) for at-a-glance metrics. Built the Time-Off tab from a reference screenshot with a metrics grid, live status widget, Create Time Off modal, and Leave History modal — including business logic to prevent creating leave when on leave and restricting to emergency-only when leaves are exhausted. Removed the unused Communication tab.

---

#### 1. Champion Detail — Welfare Notes Tab

**Files Modified:**

1. `src/data/mockChampionDetails.ts`
   * Added `WelfareNote` exported interface: `id`, `date`, `loggedBy`, `channel` (Phone Call | SMS | WhatsApp | In-Person), `interactionType` (Routine Check-In | Incident Follow-up | Welfare Complaint), `summary`, `issuesRaised`, `actionTaken`, `followUpRequired`, `incidentStatus` (Open | In-Progress | Resolved)
   * Added `welfareNotes: WelfareNote[]` to `ChampionDetails` interface
   * Champion "1" (Adewale): 5 mock welfare notes spanning May–Jun 2026 with varied channels, types, and statuses
   * Champion "2" (Chinedu): 3 mock welfare notes

2. `src/pages/ChampionDetailPage.tsx`
   * Added `WelfareNote` type import and icon imports (`Phone`, `MessageSquare`, `MessageCircle`, `User`)
   * Added variant maps: `channelIcons`, `incidentStatusVariantMap`, `interactionTypeVariantMap`
   * Welfare Notes tab renders each note as a card showing:
     - Header: date, logged by, interaction type badge, incident status badge
     - Channel icon + label, "Follow-up Required" warning pill when applicable
     - Conversation summary text
     - Issues Raised / Action Taken in a two-column footer (conditionally shown)
   * Cards have `hover:border-gray-950 transition-colors` matching `StatCard` hover behavior

---

#### 2. Champion Detail — Welfare Notes Summary Widget

**Files Modified:**

1. `src/pages/ChampionDetailPage.tsx`
   * Added summary widget at top of Welfare Notes tab with `bg-[#f9f8f6] border-[#f3f3f3]` background (matching Biodata/Contracts/Wallet top widgets)
   * Shows **Total Notes** count and **Needs Follow-up** count (warning color) separated by a vertical divider
   * Note list uses `gap-5` spacing between cards

---

#### 3. Champion Detail — Time-Off Tab (Full Build-out)

**Files Modified:**

1. `src/data/mockChampionDetails.ts`
   * Added `timeOff` object to `ChampionDetails` interface with:
     - Metrics: `leavesAvailable`, `leavesEarned`, `eligibleLeavesTaken`, `emergencyLeavesTaken`
     - `currentStatus`: `"none"` | `"on-leave"` | `"no-available"` for live status widget
     - `currentLeave?`: `{ startDate, endDate }` when on leave
     - `history`: array of `{ id, type, startDate, endDate, status, approvedBy }`
   * Champion "1": `currentStatus: "none"`, 26 leaves available, 5 history entries (mix of Approved, Pending, Declined)
   * Champion "2": `currentStatus: "on-leave"` with active leave 7–12 Jun 2026, 18 leaves available, 3 history entries

2. `src/pages/ChampionDetailPage.tsx`
   * Added imports: `useState`, `Button`, `Dialog`/`DialogContent`/`DialogHeader`/`DialogFooter`/`DialogTitle`/`DialogDescription`, `Select`/`SelectContent`/`SelectItem`/`SelectTrigger`/`SelectValue`, `Textarea`, `Input`, `Plus`, `History`, `ChampionDetails` type
   * Added state: `showCreateTimeOff`, `showLeaveHistory`, `timeOffForm`
   * Added column definitions: `leaveHistoryColumns` (4 cols: Type badge, Duration, Status badge, Approved By)
   * Added variant maps: `leaveStatusVariantMap`, `leaveTypeVariantMap`
   * **Header bar**: `bg-[#f9f8f6]` with "Time Off Schedule" title (sentence case), two `Button` CTAs:
     - "See Leave History" (`variant="outline"`, `size="sm"`) → opens leave history dialog
     - "Create Time Off" (`size="sm"`) → opens create modal
   * **Status row**: Conditionally renders one of three states:
     - `"none"` → bordered uppercase pill "No Open Time-Off Request Exists"
     - `"on-leave"` → green `StatusBadge` with date range
     - `"no-available"` → warning `StatusBadge`
   * **Metrics grid**: 3-column CSS grid with bordered cells, `py-7` vertical spacing:
     - Row 1: Eligible leaves available, Eligible leaves earned, Eligible leaves taken
     - Row 2: Emergency leaves taken
     - Labels: `font-medium text-table-text`, values: `text-3xl font-bold` zero-padded
   * **Business logic**:
     - "Create Time Off" button `disabled` when `currentStatus === "on-leave"`
     - When `leavesAvailable === 0`: form pre-selects "Emergency", Select only shows Emergency option (Annual/Sick hidden)
   * **Create Time Off modal** (`max-w-sm`):
     - Leave Type select (`w-full` trigger), Start/End date inputs, Reason textarea
     - Submit disabled until type + both dates filled
   * **Leave History modal** (`max-w-2xl`):
     - `DataTable` with `leaveHistoryColumns` showing full leave history

---

#### 4. Champion Detail — Tickets Summary Widget

**Files Modified:**

1. `src/pages/ChampionDetailPage.tsx`
   * Added summary widget above ticket table in Tickets tab
   * Shows **Total Tickets** count and **Open** count (tickets with status "Open" or "In Progress")
   * Uses same `bg-[#f9f8f6] border-[#f3f3f3]` warm background with divider

---

#### 5. Champion Detail — FieldOps Summary Widget

**Files Modified:**

1. `src/pages/ChampionDetailPage.tsx`
   * Added summary widget above FieldOps timeline
   * Shows **Total Field Operations** count
   * Uses same `bg-[#f9f8f6] border-[#f3f3f3]` warm background

---

#### 6. Champion Detail — Communication Tab Removed

**Files Modified:**

1. `src/pages/ChampionDetailPage.tsx`
   * Removed `<TabsTrigger value="communication">` from tab list
   * Removed `<TabsContent value="communication">` and its placeholder content

---

### Version [1.6.0] - 2026-06-05
**Feature: Champion 360 Enhancements, GSAP Animations & Create Ticket Vehicle Context**

**Summary:**
Enhanced the Champion 360 overview page with stat cards and expanded mock data (12→40 champions) with working pagination. Added the MaxIDCard component to the Biodata tab on the Champion Detail Page. Introduced GSAP-powered count-up animations on the Contracts and Wallet tabs (percentage counter, progress bar sweep, balance counter) using IntersectionObserver to ensure animations play only once when the tab first becomes visible — not on remount from tab switching. Added drag-to-scroll behavior on the detail page tab list for mouse users. Added conditional VehicleOverviewCard to the Create Ticket wizard's right-side widget stack for vehicle-related ticket categories.

---

#### 1. Champion 360 Overview — Stat Cards & Expanded Data

**Files Modified:**

1. `src/pages/Champion360Page.tsx`
   * Added `StatCard` import from `@/components/max`
   * Added 28 new mock champion records (IDs 13–40) with varied Nigerian names, multiple states (Lagos, Abuja, Kano, Ibadan, Port Harcourt), mixed outstanding balances, and spread of activity dates
   * Computed stats from mock data at module scope: `totalChampions`, `fullyPaidCount`, `balanceDueCount`, `totalOutstanding`
   * 4 stat cards in `grid grid-cols-4 gap-2` between PageHeader and table:
     - Total Champions (brand-primary indicator, "Across all locations" subtitle, +2.5% trend)
     - Fully Paid (success indicator, percentage subtitle, +4.2% trend)
     - Balance Due (warning indicator, percentage subtitle, -1.8% trend)
     - Total Outstanding (danger indicator, formatted as ₦ currency)
   * Wired pagination to actually slice data: added `paginatedChampions` useMemo that slices `filteredChampions` by `currentPage` and `pageSize`; DataTable now receives `paginatedChampions` instead of full list
   * Page resets to 1 when filters or search query change

---

#### 2. Champion Detail — MaxIDCard on Biodata Tab

**Files Modified:**

1. `src/data/mockChampionDetails.ts`
   * Added `maxIdCard: { variant: "active" | "inactive"; dateGenerated?: string; generatedBy?: string }` to `ChampionDetails` interface
   * Champion "1" (Adewale Ogunleye): active variant, generated 18 Mar 2024 by Samson Oluwaseun
   * Champion "2" (Chinedu Okafor): inactive variant (no card generated)

2. `src/pages/ChampionDetailPage.tsx`
   * Added `MaxIDCard` import from `@/components/max`
   * Rendered `<MaxIDCard>` at the top of the Biodata tab content, inside the card container above the Personal Information section

---

#### 3. Champion Detail — Tab List Drag-to-Scroll

**Files Modified:**

1. `src/pages/ChampionDetailPage.tsx`
   * Added `useRef` and `useCallback` imports
   * Added `tabsScrollRef` and `dragState` refs for tracking mouse drag state
   * Added `onMouseDown`, `onMouseUp`, `onMouseMove` handlers implementing click-and-drag horizontal scrolling
   * Tab list wrapper div: added `ref`, mouse event handlers, `cursor-grab`, `select-none` classes
   * Kept `scrollbar-hide` for hidden scrollbar with `overflow-x-auto` for trackpad/touch/shift+scroll

---

#### 4. Champion Detail — Tickets Tab with DataTable

**Files Modified:**

1. `src/data/mockChampionDetails.ts`
   * Added `import type { TicketRecord }` from `@/data/mockTicketRecords`
   * Added `tickets: TicketRecord[]` field to `ChampionDetails` interface
   * Added 5 mock tickets for champion "1" and 4 mock tickets for champion "2"

2. `src/pages/ChampionDetailPage.tsx`
   * Added imports for `TicketRecord`, `statusVariantMap as ticketStatusVariantMap`, `priorityVariantMap`, `slaVariantMap`
   * Defined `ticketColumns` (7 columns: Ticket ID, Category, Assigned Agent, Priority, Status, SLA, Date Created)
   * Replaced Tickets tab placeholder with `<DataTable>` in a table wrapper matching the Wallet Transactions pattern

---

#### 5. GSAP Animations — Contract & Wallet Tabs

**Dependencies Added:**
* `gsap` — installed via npm

**Files Modified:**

1. `src/components/max/ContractInformation.tsx`
   * Added GSAP import and IntersectionObserver-based animation trigger
   * Percentage counter animates from 0 to target value over 1.2s (`power2.out` easing)
   * Days elapsed text animates in sync with percentage counter
   * Progress bar: 136 bars start gray, each filled bar transitions to active color with staggered delay creating a left-to-right sweep (proportional delay `(i / filledBars) * 0.8s`)
   * `hasAnimated` ref + IntersectionObserver ensures animation plays only once when element becomes visible (not on mount while hidden)

2. `src/components/max/WalletInformation.tsx`
   * Added GSAP import and IntersectionObserver-based animation trigger
   * Available Balance counts from ₦0 to actual value over 1.2s (`power2.out` easing), formatted with commas throughout
   * Same `hasAnimated` + IntersectionObserver pattern as ContractInformation

3. `src/pages/ChampionDetailPage.tsx`
   * Added `forceMount` prop and `data-[state=inactive]:hidden` class to Contracts and Wallet TabsContent
   * `forceMount` keeps components in the DOM across tab switches so `hasAnimated` refs persist
   * `data-[state=inactive]:hidden` hides inactive tabs via `display: none`
   * IntersectionObserver in the components detects transition from hidden to visible, triggering animation only on first visibility

---

#### 6. Create Ticket — Conditional VehicleOverviewCard

**Files Modified:**

1. `src/pages/create-ticket/StepTicketDetails.tsx`
   * Added `VehicleOverviewCard` import from `@/components/max`
   * Defined `vehicleRelatedCategoryIds` Set: `cat-1` (Incident Safety), `cat-5` (Telematics), `cat-7` (Fleet), `cat-11` (Reactivation)
   * Conditionally renders `<VehicleOverviewCard>` in the right column (col-span-2) between the Champion Card and Selection Summary
   * Shows: Asset Type, Manufacturer, Contract Status (StatusBadge), Last Pinged — sourced from `champion.vehicle`
   * Styled with `className="bg-gray-50 border-gray-100"` to match surrounding widgets
   * Only visible when the selected ticket category is vehicle-related

---

### Version [1.5.0] - 2026-06-05
**Feature: MaxIDCard Component — Active & Inactive Variants**

**Summary:**
Created the `MaxIDCard` reusable component based on Figma design (node `3044:2345`). The component displays a champion's MAX ID Card status with a decorative seigaiha wave pattern header using the project's `pattern.svg` asset. Supports two variants: Active (showing generation details and View/Export actions) and Inactive (showing a message and Generate action when no ID card exists).

---

#### 1. MaxIDCard Component

**Files Created:**

1. `src/components/max/MaxIDCard.tsx`
   * `MaxIDCardVariant` exported type: `"active" | "inactive"`
   * `MaxIDCardProps` interface: `variant`, `dateGenerated`, `generatedBy`, `onViewCard`, `onExportCard`, `onGenerateCard`, `className`
   * **Header**: 70px tall div with `bg-[rgba(255,248,203,0.3)]` tint, `background-image` pointing to `/images/pattern.svg`, `background-size: 390px 390px` (65% of native 600×600), `background-repeat: repeat`
   * **Active variant**:
     - Title "MAX ID Card" at `text-[28px] font-bold` with `StatusBadge variant="success"` ("Active")
     - Two detail columns: "Date Generated" and "Generated By" (labels `text-[#a0a1a1] text-[12px]`, values `text-black text-[14px]`)
     - Two action buttons: "View ID Card" and "Export ID Card" (white bg, rounded-lg, `text-[12.5px] text-[#121314]`)
   * **Inactive variant**:
     - Same title with `StatusBadge variant="neutral"` ("Inactive")
     - Message: "No ID card has been generated for this champion" in `text-[#a0a1a1]`
     - Single action button: "Generate ID Card"
   * Badge aligned with `pt-2` wrapper to sit properly against 28px title

**Files Modified:**

2. `src/components/max/index.ts`
   * Added export: `export { MaxIDCard, type MaxIDCardVariant } from "./MaxIDCard"`

---

### Version [1.4.0] - 2026-06-04
**Feature: Create Ticket Wizard — Full 4-Step Build-out & Refinements**

**Summary:**
Built out the Create Ticket page from a stub into a full 4-step wizard at `/ticket-management/create`. The wizard walks users through champion selection, category selection, subcategory selection, and ticket detail entry using a `useReducer` state machine. Includes 18 rounds of visual refinements: step indicator styling, form layout spacing, location autocomplete extraction, cancel confirmation dialog, attachment file-type icons, submit loading state with toast notifications, and TicketDetailSheet improvements.

---

#### 1. Wizard Type Definitions

**Files Created:**

1. `src/pages/create-ticket/types.ts`
   * `WizardStep` = `1 | 2 | 3 | 4`
   * `TicketCategory` interface: `id`, `name`, `icon` (Lucide name string), `subcategoryCount`
   * `TicketSubcategory` interface: `id`, `categoryId`, `name`, `concernedTeam`, `slaTime`, `priorityLevel` (`"High" | "Medium" | "Low"`)
   * `TicketDetailsForm` interface: `platform`, `reporter`, `priority`, `city`, `locationDescription`, `date` (`Date | undefined`), `time`, `incidentDescription`, `attachments` (`File[]`)
   * `WizardState` interface: `currentStep`, `championSearch`, `selectedChampion`, `categorySearch`, `selectedCategory`, `selectedSubcategory`, `details`
   * `WizardAction` discriminated union with 11 action types: `SET_STEP`, `SET_CHAMPION_SEARCH`, `SELECT_CHAMPION`, `CLEAR_CHAMPION`, `SET_CATEGORY_SEARCH`, `SELECT_CATEGORY`, `SELECT_SUBCATEGORY`, `UPDATE_DETAILS`, `ADD_ATTACHMENT`, `REMOVE_ATTACHMENT`, `RESET`

---

#### 2. Mock Data — Categories & Subcategories

**Files Modified:**

1. `src/data/mockTicketCategories.ts`
   * Fully populated with 12 categories: Incident Safety, Phone Number Update, Documentation, Portfolio, Telematics, Prospect, Fleet, Reverification, Leave Request, Others, Reactivation, HMO/Insurance
   * Each category has a Lucide icon name string and subcategory count
   * 30 subcategories with `categoryId`, `concernedTeam`, `slaTime`, `priorityLevel`
   * Helper function: `getSubcategoriesByCategoryId(categoryId: string): TicketSubcategory[]`

---

#### 3. Step Indicator Component

**Files Created:**

1. `src/pages/create-ticket/StepIndicator.tsx`
   * Horizontal progress bar with 4 numbered circles connected by lines
   * Labels: "Select Champion", "Select Category", "Select Subcategory", "Ticket Details"
   * Circle size: `h-6 w-6`, font `text-xs`
   * **Completed step**: yellow-tint circle (`bg-brand-primary/10`) with dark check icon (`text-brand-dark`, `h-3 w-3`)
   * **Active step**: `bg-brand-dark text-white`
   * **Pending step**: `bg-gray-100 text-breadcrumb-root`
   * Connector lines: completed = `bg-gray-300`, pending = `bg-gray-200`
   * Centered with `max-w-2xl mx-auto`

---

#### 4. Wizard Footer Component

**Files Created:**

1. `src/pages/create-ticket/WizardFooter.tsx`
   * Props: `currentStep`, `isNextEnabled`, `isSubmitting`, `onBack`, `onNext`, `onCancel`, `onSubmit`
   * Layout: `border-t border-divider py-4 mt-6`, flex with justify-between
   * Left: Cancel button (`variant="outline"`)
   * Right: Back button (visible when step > 1, `variant="outline"`) + Next or Submit Ticket
   * Primary button: `bg-brand-dark text-white hover:bg-brand-dark/90`, disabled when `!isNextEnabled`
   * Submit state: shows `Loader2` spinner icon with "Submitting..." text, disables Cancel/Back buttons

---

#### 5. Step 1 — Select Champion

**Files Created:**

1. `src/pages/create-ticket/StepSelectChampion.tsx`
   * Search input with `Search` icon, placeholder "Search champion by name or ID..."
   * Filters `Object.values(mockChampionDetails)` by `name` or `championId`
   * Dropdown list: avatar (40×40), name, champion ID + location
   * On selection: renders `ChampionInformation` component with "Change" button to clear
   * Empty state: "No champions found matching..." message
   * Reuses: `ChampionInformation`, `Input`, `Button`, `mockChampionDetails`

---

#### 6. Step 2 — Select Category

**Files Created:**

1. `src/pages/create-ticket/StepSelectCategory.tsx`
   * Search input with "Search categories..." placeholder
   * 3-column grid (`grid grid-cols-3 gap-3`) of category cards
   * Each card: Lucide icon (mapped via local `iconMap` object), category name, subcategory count
   * Selected card: `border-brand-dark bg-brand-dark/5`
   * Unselected card: `border-gray-200 bg-gray-50`, hover `border-gray-300`
   * Icon imports: ShieldAlert, Phone, FileText, Briefcase, Radio, UserPlus, Car, ScanSearch, CalendarOff, MoreHorizontal, RefreshCw, HeartPulse
   * Empty state message for no matches

---

#### 7. Step 3 — Select Subcategory

**Files Created:**

1. `src/pages/create-ticket/StepSelectSubcategory.tsx`
   * Heading: "Select a subcategory under {category.name}"
   * Local search state for filtering
   * Vertical list of selectable rows from `getSubcategoriesByCategoryId(category.id)`
   * Each row: subcategory name (left), metadata on right: "Team: {concernedTeam}", "SLA: {slaTime}", `StatusBadge` for priority (using `priorityVariantMap`)
   * Selected row: `border-brand-dark bg-brand-dark/5`
   * Row style: `border border-gray-200 rounded-lg p-4 cursor-pointer hover:bg-gray-50`

---

#### 8. Step 4 — Ticket Details

**Files Created:**

1. `src/pages/create-ticket/StepTicketDetails.tsx`
   * Two-column layout: `grid grid-cols-5 gap-6`
   * **Left column (col-span-3)** — 3 bordered sections (`rounded-lg border border-gray-200 p-5`):
     - "Ticket Information": Platform (Select — Phone Call, WhatsApp, In-App, Walk-in, Email), Reporter (Input), Priority (Select — High, Medium, Low, pre-filled from subcategory), City (Select — Lagos, Abuja, Kano, Ibadan, Port Harcourt). Grid uses `gap-x-4 gap-y-6`.
     - "Incident Details": `LocationAutocomplete` for location, Date (`DatePickerField`), Time (Input type="time"), Incident Description (Textarea `rows={8}`). Children wrapped in `space-y-6 mt-2`.
     - "Attachments": `DocDropZone` accepting `.pdf,.doc,.docx,.jpg,.jpeg,.png`. File list below with type-specific icons (Image, FileText, FileSpreadsheet, File) mapped by extension, with remove button per file.
   * **Right column (col-span-2):**
     - Champion card: white background (`bg-white border border-[#f3f3f3]`), avatar (54×54), name, risk level badge, phone number, contract status (`StatusBadge`). Compact padding `p-3`.
     - "Selection Summary" `InfoCard`: `InfoGrid` showing Champion, Category, Subcategory, Concerned Team, SLA, Priority (`StatusBadge`)
     - "Ticket Script Preview" `InfoCard`: dynamic sentence "Champion {name} reported a {subcategory} issue via {platform} at {location} on {date}. {description}"
   * File icon mapping: jpg/jpeg/png → Image (yellow), pdf → FileText (red), doc/docx → FileText (blue), xls/xlsx/csv → FileSpreadsheet (green), other → File (gray)
   * Reuses: `FormSection`, `FormField` from `@/pages/vehicles/FormControls`, `DatePickerField`, `DocDropZone`, `InfoCard`, `InfoGrid`, `StatusBadge`, `LocationAutocomplete` from `@/components/max`

---

#### 9. Orchestrator Page

**Files Created:**

1. `src/pages/create-ticket/CreateTicketPage.tsx`
   * Default export, uses `useReducer` with `wizardReducer` and `initialState`
   * **Step validation** (`isNextEnabled`):
     | Step | Rule |
     |------|------|
     | 1 | `selectedChampion !== null` |
     | 2 | `selectedCategory !== null` |
     | 3 | `selectedSubcategory !== null` |
     | 4 | platform, reporter, priority, city, date, incidentDescription all non-empty |
   * **Reducer key transitions:**
     - `SELECT_CATEGORY` → resets `selectedSubcategory` to null
     - `SELECT_SUBCATEGORY` → pre-fills `details.priority` from subcategory's `priorityLevel`
   * **Cancel flow**: `showCancelDialog` state triggers `ConfirmModal` (variant `"warning"`) — "Cancel ticket creation?" with "Yes, cancel" / "Continue editing" actions
   * **Submit flow**: `isSubmitting` state → `Loader2` spinner on button → 1.5s timeout → `toast.success("Ticket created successfully")` → navigate to `/ticket-management`
   * **Layout**: `TopBar` (breadcrumbs: Driver Experience > Ticket Management (linked) > Create Ticket), `BackButton` + title, `StepIndicator`, content in `max-w-3xl mx-auto mt-6`, `WizardFooter` inside content container

2. `src/pages/CreateTicketPage.tsx`
   * Barrel re-export: `export { default } from "./create-ticket/CreateTicketPage"`

---

#### 10. LocationAutocomplete — Extracted as Reusable Component

**Files Created:**

1. `src/components/max/LocationAutocomplete.tsx`
   * Props: `value`, `onChange`, `suggestions?` (defaults to 23 Nigerian city addresses), `placeholder?`, `className?`
   * MapPin icon inside the input (`pl-9`) and beside each suggestion row
   * Click-outside-to-close via `useRef` + `useEffect`
   * Dropdown: `z-20`, `max-h-48 overflow-y-auto`, white background with `shadow-md`
   * Default suggestions span Lagos (13), Abuja (4), Kano (1), Ibadan (2), Port Harcourt (3)

**Files Modified:**

2. `src/components/max/index.ts`
   * Added export: `export { LocationAutocomplete, type LocationAutocompleteProps } from "./LocationAutocomplete"`

---

#### 11. Toast Notifications — Sonner Integration

**Files Modified:**

1. `src/components/max/AppLayout.tsx`
   * Added `import { Toaster } from "sonner"`
   * Wrapped return in `<>` fragment with `<Toaster position="top-right" richColors />` before `PageLayout`

---

#### 12. TicketDetailSheet Refinements

**Files Modified:**

1. `src/components/max/TicketDetailSheet.tsx`
   * **Width**: Added `max-w-[40vw]` to `SheetContent` className (limits sheet to 40% of viewport)
   * **Attachment file-type icons**: Replaced plain text attachment tags with icon-enhanced tags
     - `attachmentIconMap` record mapping extensions to `{ icon: LucideIcon; color: string }`:
       - jpg/jpeg/png → `Image` + `text-status-warning` (yellow)
       - pdf → `FileText` + `text-status-danger` (red)
       - doc/docx → `FileText` + `text-status-info` (blue)
       - xls/xlsx/csv → `FileSpreadsheet` + `text-badge-active-text` (green)
       - Default fallback: `File` + `text-breadcrumb-root` (gray)
   * Added Lucide imports: `Image`, `FileText`, `FileSpreadsheet`, `File`

---

#### 13. ConfirmModal — Warning & Success Variants

**Files Modified:**

1. `src/components/max/ConfirmModal.tsx`
   * Extended `ConfirmModalVariant` type: `"destructive" | "default" | "warning" | "success"`
   * **Warning variant**: `bg-status-warning/10` icon circle, `bg-status-warning` primary button, renders Lucide icon in tinted circle
   * **Success variant**: renders `success_Checkmark.svg` image (no icon circle), `bg-brand-dark` primary button
   * **Destructive variant**: renders `rejected.png` image (no icon circle), `bg-status-danger` primary button
   * **Default variant**: `bg-status-info/10` icon circle, `bg-brand-dark` primary button

---

#### 14. Mock Ticket Detail — Attachment Type Expansion

**Files Modified:**

1. `src/data/mockTicketDetail.ts`
   * Widened attachment `type` field from `"jpg" | "pdf" | "png"` to `string`
   * Ticket 1 (`TK-2024-001`): added `incident_report.pdf` (pdf) and `mechanic_notes.docx` (docx)
   * Ticket 2 (`TK-2024-002`): added `transaction_log.xlsx` (xlsx)

---

#### 15. Route & Navigation Wiring

**Files Modified:**

1. `src/App.tsx`
   * Added import: `import CreateTicketPage from "@/pages/CreateTicketPage"`
   * Added route: `<Route path="/ticket-management/create" element={<CreateTicketPage />} />`

2. `src/pages/TicketManagementPage.tsx`
   * Added `import { useNavigate } from "react-router-dom"`
   * Added `const navigate = useNavigate()` inside component
   * Added `onClick={() => navigate("/ticket-management/create")}` to "Create Ticket" `<Button>`

---

#### 16. Documentation Update

**Files Modified:**

1. `docs/components.md`
   * Added component documentation for: `LocationAutocomplete`, `DatePickerField`, `DocDropZone`, `InfoCard`, `InfoGrid`, `ChampionInformation`
   * Updated `ConfirmModal` documentation with `warning` and `success` variant styles
   * Added `TicketDetailSheet` section with attachment icon mapping and 40vw max width
   * Added "Create Ticket Wizard" page pattern documentation (file structure, steps, state management, layout, behaviors)
   * Added "Toast Notifications" section documenting sonner integration
   * Updated component architecture tree and barrel import example
   * Added 9 changelog entries dated 2026-06-01

---

### Version [1.3.0] - 2026-06-04
**Feature: Drivers Safety Performance Page — Full Build-out & Naming Alignment**

**Summary:**
Built out the Drivers Safety Performance page from its skeleton state into a complete dashboard with 7 stat cards, 2 chart widgets, and a filterable driver risk table. Created the supporting mock data file. Aligned the page name across the app — sidebar label, breadcrumbs, page header, and vehicle table column header all now read "Driver(s) Safety Performance".

---

#### 1. Mock Data File

**Files Created:**

1. `src/data/mockDriverSafety.ts`
   * `RiskLevel` type: `"Low" | "Medium" | "High" | "Critical"`
   * `DriverRiskRecord` interface: `id`, `championName`, `championId`, `location`, `safetyScore` (0–100), `riskLevel`, `recentIncident`, `totalSafetyEvents`, `lastActivity`
   * `CriticalEventCategory` interface: `name`, `count`, `color`
   * `BehavioralTrendPoint` interface: `date`, `events`
   * `riskLevelVariantMap`: Low→success, Medium→warning, High→danger, Critical→info
   * `mockDriverRiskRecords`: 15 records with Nigerian names, locations (Lagos, Abuja, Kano, Ibadan, Port Harcourt)
   * `mockCriticalEvents`: 6 categories (Collision, Speeding, Harsh Braking, Night Driving, Sharp Cornering, Idling Abuse) with color tokens
   * `mockBehavioralTrend`: 15 date points spanning 30 days

---

#### 2. Drivers Safety Performance Page — Full Build-out

**Files Modified:**

1. `src/pages/DriverSafetyScorePage.tsx`
   * **Global Period Filter** — `Select` dropdown (Last 7/30/90 Days) aligned right of the `PageHeader`
   * **7 Stat Cards** in `grid grid-cols-4` layout (wraps to 4+3 rows):
     - No of Active Drivers (green)
     - Drivers Online (cyan)
     - Average Safety Score (blue)
     - % Safe (green)
     - % At-Risk (orange)
     - Total Safety Events (red)
     - High Risk Alerts (red)
     - All values derived from `mockDriverRiskRecords` at module scope
   * **Widget 1: Critical Events (48-Hour Window)** — Recharts `BarChart` with per-bar colors via `Cell`, hover opacity effect, `DarkTooltipContent` tooltip
   * **Widget 2: Incidents by Category** — Category breakdown list with colored dot, name, count, proportional progress bar, and percentage of total incidents
   * **Driver Risk View Table** — `DataTable` with 7 columns:
     - Champion name + ID (stacked layout)
     - Location
     - Safety Score (color-coded: green ≥80, orange ≥60, red <60)
     - Risk Level (`StatusBadge` pill using `riskLevelVariantMap`)
     - Recent Incident
     - Total Safety Events
     - Last Activity
   * **Filter toolbar**: `GenericFilterPopover` with 4 sections:
     - Location (Lagos, Abuja, Kano, Ibadan, Port Harcourt)
     - Risk Level (Low, Medium, High, Critical with color indicators)
     - Safety Score (discrete range buckets: 0–40, 41–60, 61–80, 81–100)
     - Total Events (discrete range buckets: 0–5, 6–10, 11–20, 21+)
   * **Search toggle** — Button→Input with Escape key to close
   * **Pagination** component below table
   * Helper `matchesRange()` function for range-bucket filter logic

---

#### 3. Naming Alignment

**Files Modified:**

1. `src/data/sidebarConfig.ts`
   * Line 267: Changed sidebar label from `"Driver Safety Score"` to `"Drivers Safety Performance"`

2. `src/pages/DriverSafetyScorePage.tsx`
   * Breadcrumb updated to "Driver Experience > Drivers Safety Performance"
   * Page title updated to "Drivers Safety Performance"

3. `src/pages/vehicles/columns.tsx`
   * Line 137: Changed column header from `"Driver Safety Score"` to `"Driver Safety Performance"`

---

#### 4. Driver Detail Sheet — Table Row Click

**Files Created:**

1. `src/components/max/DriverDetailSheet.tsx`
   * Props: `driver` (`DriverRiskRecord | null`), `isOpen`, `onClose`
   * Sheet layout: `max-w-[40vw]`, follows `TicketDetailSheet` pattern
   * Header: Champion name + Risk Level `StatusBadge`, description shows Champion ID and location
   * 4 `InfoCard` sections:
     - **Champion Profile**: Avatar (54×54), name, ID, location, last activity via `InfoGrid`
     - **Safety Overview**: Safety score (color-coded), risk level badge, total events, recent incident via `InfoGrid`
     - **Score Breakdown**: Visual progress bar (0–100) with color thresholds (green ≥80, orange ≥60, red <60) and scale labels
     - **Activity Summary**: Full 6-field `InfoGrid` (Champion ID, Location, Last Active, Total Events, Most Recent Incident, Risk Classification)

**Files Modified:**

2. `src/components/max/index.ts`
   * Added export: `export { DriverDetailSheet } from "./DriverDetailSheet"`

3. `src/pages/DriverSafetyScorePage.tsx`
   * Added `selectedDriverId` state and `selectedDriver` derived lookup
   * Added `onRowClick={(row) => setSelectedDriverId(row.id)}` to `DataTable`
   * Renders `DriverDetailSheet` at page bottom, closes on `onClose`

---

#### 5. Incident Champions Sheet — Bar Chart Click

**Files Created:**

1. `src/components/max/IncidentChampionsSheet.tsx`
   * Props: `incident` (`CriticalEventCategory | null`), `isOpen`, `onClose`
   * Filters `mockDriverRiskRecords` by `recentIncident === incident.name`
   * Header: colored dot, incident name, event count badge, description text
   * Champion cards: avatar (40×40), name, ID, risk level `StatusBadge`, 3-column grid (Location, Safety Score color-coded, Total Events)
   * Empty state: centered message when no champions match

**Files Modified:**

2. `src/components/max/index.ts`
   * Added export: `export { IncidentChampionsSheet } from "./IncidentChampionsSheet"`

3. `src/pages/DriverSafetyScorePage.tsx`
   * Added `selectedIncident` state (`CriticalEventCategory | null`)
   * Added `onClick` handler on bar chart `Bar` component — finds matching `CriticalEventCategory` by `name` and sets state
   * Renders `IncidentChampionsSheet` at page bottom, closes on `onClose`

---

### Version [1.2.0] - 2026-06-04
**Feature: Driver Experience App — Full Module Build-out**

**Summary:**
Built the entire Driver Experience app module from scratch — sidebar navigation, 4 main pages (Champion 360, Ticket Management, Drivers Safety Performance, Welfare & Performance), 2 sub-pages (Champion Detail, Create Ticket), 5 new reusable components, 4 mock data files, and a Figma design system component with token sheet.

---

#### 1. Driver Experience Sidebar & App Switching

**Files Modified:**

1. `src/data/sidebarConfig.ts`
   * Added `driverExperienceSidebarSections` export with 4 sidebar items:
     - Champion 360 (`/champion-360`, icon: `agent_menu.svg`)
     - Ticket Management (`/ticket-management`, icon: `issues_menu.svg`)
     - Drivers Safety Performance (`/driver-safety-score`, icon: `driver_safety_menu.svg`)
     - Welfare & Performance (`/welfare-performance`, icon: `fleet_performance_menu.svg`)

2. `src/components/max/AppLayout.tsx`
   * Imported `driverExperienceSidebarSections` from sidebarConfig
   * Added ternary condition for `selectedAppId === "driver-experience"` in sections selector
   * Added `"driver-experience": "/champion-360"` to `appDefaultRoutes`

3. `src/App.tsx`
   * Added imports for all 6 new page components
   * Added 6 new `<Route>` elements for Driver Experience pages
   * Added `Agentation` dev tool component (renders only in dev mode)

---

#### 2. Champion 360 Page

**Files Created:**

1. `src/pages/Champion360Page.tsx`
   * DataTable with 6 columns: Champion (avatar + name + ID), Contact Number, Location, State, Plate Number, Outstanding Balance (color-coded: orange > 0, green = 0), Last Active Date
   * 12 rows of mock champion data with Nigerian names, phone numbers, locations, plate numbers
   * `GenericFilterPopover` with Location and State filter sections
   * Search bar with keyboard shortcut (Escape to close)
   * Pagination component
   * Row click navigates to `/champion-360/:id`

---

#### 3. Champion Detail Page

**Files Created:**

1. `src/pages/ChampionDetailPage.tsx`
   * Individual champion detail view at `/champion-360/:id`
   * Uses `ChampionInformation`, `ContractInformation`, and `WalletInformation` card components
   * Tabbed interface with FieldOps History using `StatusTimeline`

---

#### 4. Ticket Management Page

**Files Created:**

1. `src/pages/TicketManagementPage.tsx`
   * 6 stat cards: Open Tickets, In Progress, SLA Breached, Received Today, Pending Feedback, Closed
   * DataTable with 10 columns: Ticket ID, Affected Champion, Category, Location, Assigned Agent, Ticket Creator, Priority (StatusBadge), Status (StatusBadge), SLA (StatusBadge), Date Created
   * `GenericFilterPopover` with 5 filter sections: Status, Priority, Category, Assignee, SLA
   * Search bar for ticket ID, champion, or agent
   * Row click opens `TicketDetailSheet` slide-over panel
   * "Create Ticket" button navigates to `/ticket-management/create`

2. `src/pages/CreateTicketPage.tsx`
   * Ticket creation form at `/ticket-management/create`

3. `src/data/mockTicketRecords.ts`
   * `TicketRecord` interface and mock data array
   * `statusVariantMap`, `priorityVariantMap`, `slaVariantMap` variant maps

4. `src/data/mockTicketDetail.ts`
   * Detailed ticket data with `getTicketDetail()` lookup and `buildTicketDetailFromRecord()` builder

5. `src/data/mockTicketCategories.ts`
   * Ticket category definitions

---

#### 5. Drivers Safety Performance Page

**Files Created:**

1. `src/pages/DriverSafetyScorePage.tsx`
   * 7 stat cards: Active Drivers, Drivers Online, Average Safety Score, % Safe, % At-Risk, Total Safety Events, High Risk Alerts (derived from mock data)
   * 2 chart widgets:
     - Critical Events (48-Hour Window) — `BarChart` (recharts) with hover interaction and dark tooltip
     - Incidents by Category — horizontal progress bars with percentages
   * "Driver Risk View" DataTable with 7 columns: Champion name/ID, Location, Safety Score (color-coded by threshold), Risk Level (StatusBadge), Recent Incident, Total Safety Events, Last Activity
   * `GenericFilterPopover` with 4 filter sections: Location, Risk Level, Safety Score range, Total Events range
   * Search bar and pagination with server-style slicing
   * Period selector (Last 7/30/90 Days)

2. `src/data/mockDriverSafety.ts`
   * `DriverRiskRecord` interface and mock data array
   * `mockCriticalEvents` with color-coded event categories
   * `riskLevelVariantMap` for StatusBadge variants

---

#### 6. Welfare & Performance Page

**Files Created:**

1. `src/pages/WelfarePerformancePage.tsx`
   * Placeholder page with TopBar breadcrumbs and PageHeader

---

#### 7. New Reusable Components

**Files Created:**

1. `src/components/max/ChampionInformation.tsx`
   * Champion info card matching Figma design (node `3001:710`)
   * Props: `name`, `avatarUrl`, `riskLevel` (High/Medium/Low Risk), `phoneNumber`, `location`, `onboardedDate`, `lastPingedOn`, `contractStatus` (Early Arrears/Active/Inactive/Defaulting)
   * Uses Satoshi font via Tailwind `font-medium` class
   * Risk badge with conditional color: red (High), orange (Medium/Low)
   * Contract status rendered via `StatusBadge` with `contractVariantMap`
   * `DetailRow` helper component for label/value pairs

2. `src/components/max/ContractInformation.tsx`
   * Contract details card component

3. `src/components/max/WalletInformation.tsx`
   * Wallet/payment information card component

4. `src/components/max/TicketDetailSheet.tsx`
   * Slide-over sheet for viewing ticket details from table row click

5. `src/components/max/LocationAutocomplete.tsx`
   * Autocomplete input for location selection

**Files Modified:**

6. `src/components/max/index.ts`
   * Added exports for all 5 new components: `ChampionInformation`, `ContractInformation`, `WalletInformation`, `TicketDetailSheet`, `LocationAutocomplete`

---

#### 8. Figma Design System — Champion Information Component

**Figma File:** `XqgkCbETnmDwqsbXYkzDBW` (MaxOne 2.0 Design System)

**Created on Design System page:**

1. **Champion Information Component** (Figma Component, ID: `3015:28`)
   * Matches reference design at node `3001:710` on Page 1
   * Auto-layout vertical container: 328×282px, corner radius 8px
   * Champion name section: avatar (54×54 ellipse with `champvatar.png` image fill), name text, "High Risk" badge
   * 5 detail rows: Phone Number, Location, Onboarded, Last pinged on, Contract status (with "Early Arrears" warning badge)
   * Uses Inter Medium/Semi Bold as API-available substitute for Satoshi Variable

2. **Token Sheet Documentation Frame** (ID: `3022:28`)
   * Positioned at x:1524, y:-84 alongside existing Button and Badge sections
   * **Color Tokens** (10 entries with swatches): Card Background #FFFFFF, Card Border #EAEAEA, Name Section BG #FCFCFC, Name Section Border #F3F3F3, Primary Text #121314, Secondary Text #888989, High Risk Badge BG #FFECEC, High Risk Badge Text #DC2626, Warning Badge BG #FFF3E0, Warning Badge Text #F59E0B
   * **Typography** (5 entries): Champion Name (Medium 15px / -0.15), Risk Badge (Medium 11.5px / -0.115), Detail Label (Medium 13px / -0.13), Detail Value (Medium 13px / -0.13), Contract Badge (Medium 12px / -0.12)
   * **Spacing** (6 entries): Card Padding (24/20/20/20), Section Gap (16px), Detail Row Gap (12px), Card Radius (8px), Badge Radius (13px), Avatar Size (54×54px)

---

### Version [1.1.0] - 2026-06-04
**Feature: FieldOps History Tab on Champion Detail Page**

**Summary:**
Populated the FieldOps History tab on the Champion Detail Page (`/champion-360/:id`) with a vertical timeline of field operations, reusing the existing `StatusTimeline` component pattern.

**Files Modified:**

1. `src/data/mockChampionDetails.ts`
   * Imported `TimelineEntryData` type from `@/components/max`
   * Added `fieldOps: TimelineEntryData[]` to the `ChampionDetails` interface
   * Added mock field operations data for champion "1" (5 entries across 3 dates: 28 May, 20 May, 10 May 2026)
   * Added mock field operations data for champion "2" (4 entries across 3 dates: 30 May, 22 May, 15 May 2026)
   * Four operation types covered with assigned status variants:
     - Vehicle Recovery (`statusVariant: "danger"`)
     - Welfare Check (`statusVariant: "success"`)
     - Document Collection (`statusVariant: "info"`)
     - Vehicle Inspection (`statusVariant: "warning"`)

2. `src/pages/ChampionDetailPage.tsx`
   * Added `StatusTimeline` to the `@/components/max` import
   * Replaced FieldOps placeholder text with `<StatusTimeline entries={champion.fieldOps} />`
   * Added `overflow-hidden` to the `Tabs` wrapper className to fix horizontal tab scrolling constraint

**Bug Fix:**
* Fixed tab list overflow issue — the `Tabs` wrapper was missing `overflow-hidden`, allowing it to expand beyond the viewport width instead of constraining the inner scroll container. Added `overflow-hidden` to the `Tabs` className so the `overflow-x-auto` scroll wrapper works correctly.

**Components Reused (no new components created):**
* `StatusTimeline` — `src/components/max/StatusTimeline.tsx`
* `TimelineEntry` — `src/components/max/TimelineEntry.tsx`
* `TimelineEntryData` type — `src/components/max/TimelineEntry.tsx`

---

### Version [1.0.0] - 2026-06-04
* Initial workspace initialization.
